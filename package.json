{
  "name": "tdx-proof-verifier",
  "version": "1.0.0",
  "description": "TDX Quote Verification on Base Sepolia using RISC0",
  "scripts": {
    "deploy": "forge script script/Deploy.s.sol --rpc-url base_sepolia --broadcast --verify",
    "submit-proof": "node submit_proof.js",
    "build": "forge build",
    "test": "forge test"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "ethers": "^6.14.0",
    "hardhat": "^2.19.0",
    "@nomicfoundation/hardhat-toolbox": "^4.0.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-verify": "^2.0.0"
  }
}
